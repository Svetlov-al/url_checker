networks:
  dev:
    driver: bridge

services:
  redis:
    image: redis
    ports:
      - "6379:6379"
    networks:
      - dev

  celery_worker:
    container_name: celery_worker
    build: ..
    command: celery -A infra.celery_worker worker --pool=solo --loglevel=info
    volumes:
      - ../app:/src/app
    depends_on:
      - redis
    networks:
      - dev
    env_file:
      - ../.env

  flower:
    container_name: flower
    build: ..
    command: celery -A infra.celery_worker flower --port=5555
    ports:
      - "5555:5555"
    depends_on:
      - redis
      - celery_worker
    networks:
      - dev
    env_file:
      - ../.env
